@model FacultyDropDownListModel
@{
    
        @Html.DropDownListFor(x => x.SelectedValue, new SelectList(Model.Values), "Select the faculty", new { onchange = "GetGroups(this.value)", id = "faculty" })
    

}
